databaseChangeLog:
  - changeSet:
      id: 1
      author: user
      changes:
        - sql: |
            CREATE TABLE cultures (
              id bigint PRIMARY KEY,
              name VARCHAR(100) NOT NULL,
              interval_days INT NOT NULL,
              duration_minutes INT NOT NULL,
              last_watered DATE
            );

        - sql: |
            INSERT INTO cultures (id, name, interval_days, duration_minutes, last_watered) VALUES
            (1, 'виноград', 3, 1, '2025-09-01'),
            (2, 'яблоня', 5, 1, '2025-09-02'),
            (3, 'смородина', 4, 1, '2025-08-30');